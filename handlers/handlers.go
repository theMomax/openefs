package handlers

import "github.com/gin-gonic/gin"

// Register takes care of registering all handler functions to the router.
func Register(r gin.RouterGroup) {
	// TODO add routes/calls to Register of subpackages
}
